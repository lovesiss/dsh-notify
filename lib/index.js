/* built by scripts/build.mjs — do not edit directly */
/* dsh-notify · host half. The Node loader imports this entry for the row;
 * notifications are browser-side, so the host half is deliberately empty.
 * It exists so `import("dsh-notify")` resolves without touching the
 * browser-only bundle. */
export default {
  name: "dsh-notify",
  apply() {}
};
